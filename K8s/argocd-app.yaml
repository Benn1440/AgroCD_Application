apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: node-server
  namespace: argocd
spec:

  source:
    repoURL: https://github.com/Benn1440/AgroCD_Application
    targetRevision: main
    path: k8s  
  project: default

  destination:
    server: https://kubernetes.default.svc
    namespace: myappns

  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
    - CreateNamespace=true